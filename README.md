
  # DeCaires Technology Platform Layout

  This is a code bundle for DeCaires Technology Platform Layout. The original project is available at https://www.figma.com/design/xTEau2si1Yk3ge7cykfE2w/DeCaires-Technology-Platform-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  